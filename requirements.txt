# Core ML / Deep Learning
torch>=2.0.0
torchvision>=0.15.0

# PyTorch Geometric (for HeteroData KG format)
torch-geometric>=2.3.0

# Knowledge Graph Embeddings
pykeen>=1.10.0

# NLP / Embeddings
gensim>=4.3.0          # FastText (skip-gram) training via LineSentence
flashtext>=2.7         # O(n) dictionary-based entity anchoring

# Biomedical Data Access
biopython>=1.81        # NCBI Entrez API for PubMed retrieval

# Graph Processing
networkx>=3.1          # KG stored as MultiDiGraph

# Dimensionality Reduction / Visualisation
umap-learn>=0.5.3      # UMAP projection of TransE embeddings
matplotlib>=3.7.0
seaborn>=0.12.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0    # Stratified splits, AUROC, AUPRC, F1

# Utilities
tqdm>=4.65.0
requests>=2.31.0       # HTTP requests for NCBI API
tenacity>=8.2.0        # Exponential backoff for API calls
jsonlines>=3.1.0       # JSONL abstract storage
